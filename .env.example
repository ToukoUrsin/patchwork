# Public configuration. Root creates the real project and public dataset.
SANITY_PROJECT_ID=
SANITY_DATASET=production
# Server-only write token. Never prefix with NEXT_PUBLIC_. Never commit a value.
SANITY_API_TOKEN=
# Authoring is localhost-only. Remote installations remain read-only.
